import { MovieListFilterPipe } from './movie-list-filter.pipe';

describe('MovieListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
